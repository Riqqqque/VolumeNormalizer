{
  "name": "volume-normalizer",
  "version": "2.0.6",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "lint:scripts": "node --check background.js && node --check content.js && node --check popup.js && node --check scripts/build.js && node --check scripts/bump-version.js && node --check scripts/publish-chrome.js",
    "lint:firefox": "web-ext lint --source-dir dist/firefox",
    "check": "npm run build && npm run lint:scripts && npm run lint:firefox",
    "version:set": "node scripts/bump-version.js",
    "publish:chrome": "node scripts/publish-chrome.js",
    "publish:firefox": "web-ext sign --source-dir dist/firefox --artifacts-dir dist/firefox-signed --channel listed --approval-timeout 0"
  },
  "devDependencies": {
    "web-ext": "^9.4.0",
    "yazl": "^3.3.1"
  }
}
